# Workspace Notes

- This is a React + TypeScript + Vite portfolio intended for Azure Static Web Apps deployment.
- Keep edits minimal and focused on the portfolio content, styling, and deployment path.
- Build with `npm run build` before considering the task complete.
- Update the README when deployment or setup steps change.